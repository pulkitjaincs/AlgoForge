{
  "name": "algoforge",
  "private": true,
  "version": "1.0.0",
  "description": "A full-stack DSA preparation platform with progress tracking",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "author": "pulkitjaincs",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^22.0.0",
    "turbo": "^2.10.7",
    "typescript": "^5.7.3"
  }
}
